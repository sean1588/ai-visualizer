{
  "name": "mise-lambda-cook",
  "version": "0.1.0",
  "private": true,
  "description": "Mise · stateless LLM proxy Lambda",
  "main": "dist/cook.js",
  "scripts": {
    "build": "esbuild cook.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/cook.js --minify",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
