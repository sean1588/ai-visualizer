{
  "name": "mise-lambda-cook",
  "version": "0.1.0",
  "private": true,
  "description": "Mise · stateless LLM proxy Lambda",
  "main": "dist/cook.js",
  "scripts": {
    "build": "esbuild cook.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/cook.js --minify",
    "typecheck": "tsc --noEmit && npm test",
    "pretest": "npm --prefix ../frontend run build:public",
    "test": "playwright install chromium && node --experimental-strip-types --test tests/*.test.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1130.0",
    "@sean.holung/minicode-sdk": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.23.0",
    "playwright": "^1.59.1",
    "typescript": "^5.5.0"
  }
}
