{
  "name": "Mise (ai-visualizer)",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "app",
      "command": "npm --prefix frontend run build && python3 -m http.server 8080 --directory frontend/dist",
      "description": "Builds and serves the Vite frontend (frontend/dist/) at http://localhost:8080. The /api/cook and /api/fetch-data endpoints are backed by the Lambda in production."
    }
  ],
  "ports": [
    { "name": "app", "port": 8080 }
  ]
}
